{
  "name": "prezzo-libri",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.mjs",
    "test": "node --test",
    "check": "node --check server.mjs && node --check src/pricing.mjs && node --check public/app.js"
  },
  "engines": { "node": ">=24" }
}
